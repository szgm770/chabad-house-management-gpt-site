"use client";
import { Copy, CreditCard, AlertCircle, RefreshCw } from "lucide-react";
import { useState } from "react";
export default function WebhookAddresses(){const[notice,setNotice]=useState("");const base=typeof window!=="undefined"?window.location.origin:"";const rows=[{name:"נדרים פלוס · עסקאות מוצלחות",path:"/api/webhooks/nedarim",icon:CreditCard},{name:"נדרים פלוס · הקמת הוראת קבע",path:"/api/webhooks/nedarim/recurring",icon:RefreshCw},{name:"נדרים פלוס · סירובי עסקאות",path:"/api/webhooks/nedarim/declines",icon:AlertCircle},{name:"SUMIT · תנועות",path:"/api/webhooks/sumit",icon:CreditCard}];return <section className="config-panel webhook-addresses">{notice&&<div className="inline-notice">{notice}</div>}<div className="panel-head"><h2>כתובות Webhook</h2></div><div>{rows.map(r=><article key={r.path}><span><r.icon/></span><div><b>{r.name}</b><small>{base+r.path}</small></div><button onClick={()=>{void navigator.clipboard.writeText(base+r.path);setNotice(`כתובת ${r.name} הועתקה`)}}><Copy/>העתקה</button></article>)}</div></section>}
